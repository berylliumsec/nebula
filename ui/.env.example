# Browser deployments normally use the same origin and leave this unset.
VITE_NEBULA_API_URL=http://127.0.0.1:8765

# Development-only token. The Tauri shell obtains a one-time token from its
# supervised sidecar and does not read this value.
VITE_NEBULA_API_TOKEN=
