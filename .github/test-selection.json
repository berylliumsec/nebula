{
  "change_digest": "1e7eb850709cd0ea6f091e352b05df206547fd74e80160c3d7700cfb75cfe1f5",
  "reviewed_by": "Claude focused review for ctx-search-budget",
  "reason": "Per-turn allowances for conversation.search and knowledge.search (new search_allowance.py): 8 searches of one kind, or 3 empty in a row, counted from the turn ledger (_turn_history; the same ledger-count pattern as the catalog-discovery limit) against earlier routing responses only, so one response's batch runs whole. Past it the sequential routing loop answers the call itself through _refused_tool_step, which gains an ordinary-result mode (status complete, budget_class refused so no budget is spent, trusted_result so the replay carries Core's text); the existing routing-deviation limit stops a model that keeps searching. The answer points to tool_output.search for results cleared from context. Selected: the new test file; both search tools' tests; every file driving the sequential routing loop, refused steps and replay (tool loop, tool catalog incl. the discovery limit, mid-turn compaction, in-turn pruning, prompt cache, synthesis declarations, turn outcome history), restart recovery, tool-history memory and test_chat.py. Rebased onto 9bf066a. Counts from pytest --collect-only.",
  "exclusions": "No frontend, native or Playwright tests: no UI change; an answered search renders in the existing tool card as a completed call with its detail. Harness and mission paths are unchanged. Real-Core evidence (s1 retention eval, same-base comparisons on port 18719) is in the PR body. No full suite.",
  "expected_tests": {
    "python": 262,
    "frontend": 0,
    "native": 0,
    "playwright": 0
  },
  "python_versions": [
    "3.12"
  ],
  "python_browser": false,
  "python_node": false,
  "python": [
    "tests/v3/test_search_allowance.py",
    "tests/v3/test_conversation_search.py",
    "tests/v3/test_knowledge_search.py",
    "tests/v3/test_chat_tool_loop.py",
    "tests/v3/test_tool_catalog.py",
    "tests/v3/test_midturn_compaction.py",
    "tests/v3/test_in_turn_context_pruning.py",
    "tests/v3/test_turn_prompt_cache.py",
    "tests/v3/test_restart_recovery_lifecycle.py",
    "tests/v3/test_tool_history_memory.py",
    "tests/v3/test_chat.py",
    "tests/v3/test_synthesis_tool_declarations.py",
    "tests/v3/test_turn_outcome_history.py"
  ],
  "frontend": [],
  "native": [],
  "playwright": []
}
