{
  "change_digest": "c84de5fd308b9e0e80d17179a44e0243e9493b85398cd29fd66b8febcffbd338",
  "reviewed_by": "Claude review of published model context windows",
  "reason": "resolve_context_limits now falls back to a generated table of published context/output limits (src/nebula/v3/known_model_limits.py, built by scripts/refresh_known_model_limits.py) when a non-local provider has no exact catalog descriptor, reporting capacity source known_model. Catalog descriptors, configured caps and the unverified-OpenRouter 8k ceiling take precedence as before. The UI maps the new source to a 'published model limits' label. Selected tests cover limit resolution and model-id folding in test_context.py, chat compaction that consumes these limits in test_chat.py, the context-status client mapping in client.test.ts, and the assistant Playwright area. The conversation actions Playwright test is updated to step through the Archive item (added in #347) between Delete and Rename; it runs in the selected assistant area.",
  "exclusions": "No full suites. Orchestration uses the same resolve_context_limits path with local fixture profiles, which the table intentionally skips; test_orchestration.py has two compaction failures that already reproduce on main. No LAN-origin check: no origin, cookie or socket behavior changed.",
  "expected_tests": {
    "python": 72,
    "frontend": 62,
    "native": 0,
    "playwright": 4
  },
  "python": [
    "tests/v3/test_chat.py",
    "tests/v3/test_context.py"
  ],
  "frontend": [
    "ui/src/api/client.test.ts"
  ],
  "native": [],
  "playwright": [
    "area:assistant"
  ]
}
