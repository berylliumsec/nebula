{
  "change_digest": "50e26c8611941f7d1c6a5cfd660d6d1b497c2628c3affcec95c8312fa1c1de67",
  "reviewed_by": "Claude focused review for ctx-recent-window",
  "reason": "chat_turn_ledger.py: the recent tool window keeps whole response groups newest first only while they fit recent_window_tokens(working input capacity) = max(2,000, 25% of input capacity), never fewer than the newest group and still at most eight; chat.py passes that bound from _compacted_turn_history. Only which steps may fold changes. Selected: the new window tests; every file whose tool turns replay, fold, clear or checkpoint steps (in-turn pruning, prompt cache, tool history memory, mid-turn compaction, tool loop, write amplification, final synthesis recovery, routing tolerance, subagents, agent messages, synthesis declarations, restart recovery, malformed calls, turn outcome history, reasoning recovery, tool failures, subagent delivery, reference material, conversation search, goals, chat); chat context assembly and precompaction, which build the requests these turns extend. Two tests changed: the HIST-3 pruning test's 'at least eight groups whole' becomes 'at least the newest group' (the contract this PR changes), Stream X's working-ceiling test accepts results folded or cleared, and Stream D's lookup clearing-order test keeps the group-only window so its scenario still reaches clearing. Counts from pytest --collect-only.",
  "exclusions": "No frontend, native or Playwright tests: no UI, Rust or visible change; the checkpoint block the model reads keeps its schema. Provider adapters and estimation (Stream W) are untouched. Real-Core evidence (OpenRouter deepseek/deepseek-v4.1-flash batching reads of 40 keyed files on a 16K window, branch against main 517a4a5) is recorded in the PR body. No full suite.",
  "expected_tests": {
    "python": 399,
    "frontend": 0,
    "native": 0,
    "playwright": 0
  },
  "python_versions": [
    "3.12"
  ],
  "python_browser": false,
  "python_node": false,
  "python": [
    "tests/v3/test_recent_window.py",
    "tests/v3/test_in_turn_context_pruning.py",
    "tests/v3/test_turn_prompt_cache.py",
    "tests/v3/test_tool_history_memory.py",
    "tests/v3/test_midturn_compaction.py",
    "tests/v3/test_chat_tool_loop.py",
    "tests/v3/test_turn_write_amplification.py",
    "tests/v3/test_chat.py",
    "tests/v3/test_chat_goals.py",
    "tests/v3/test_chat_final_synthesis_recovery.py",
    "tests/v3/test_chat_routing_tolerance.py",
    "tests/v3/test_chat_subagents.py",
    "tests/v3/test_chat_agent_messages.py",
    "tests/v3/test_synthesis_tool_declarations.py",
    "tests/v3/test_restart_recovery_lifecycle.py",
    "tests/v3/test_malformed_tool_call_feedback.py",
    "tests/v3/test_turn_outcome_history.py",
    "tests/v3/test_reasoning_recovery.py",
    "tests/v3/test_tool_failures.py",
    "tests/v3/test_subagent_delivery.py",
    "tests/v3/test_chat_reference_material.py",
    "tests/v3/test_conversation_search.py",
    "tests/v3/test_precompaction.py",
    "tests/v3/test_chat_context_assembly.py"
  ],
  "frontend": [],
  "native": [],
  "playwright": []
}
