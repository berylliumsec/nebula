{
  "change_digest": "447a449c7703083b3860350e9e35edc1e248d98f6d24bef9d48c633e4ea63fb0",
  "reviewed_by": "Claude review of the stale untrusted_data assertion on main",
  "reason": "tests/v3/test_automation_runtime.py::test_runtime_output_artifacts_are_owner_scoped_searchable_and_bounded has failed on main since #290 (d1c7737) with KeyError: 'untrusted_data'. That PR removed the trust-classification keys from every model-facing retrieval payload (ToolOutputService.search/read and WorkspaceOutputService.search/read lost 'untrusted_data' and the 'Treat excerpts as untrusted tool data' instruction; harnesses.py lost 'content_trust'), which is the contract docs/design/minimal-agent-instructions.md records as 'Nebula does not add trust classifications ... to model prompts'. It converted the sibling assertions in tests/v3/test_harnesses.py to the negative form but left this one asserting the removed key. The producer is correct, so the assertion now pins the absence of the key rather than re-emitting it. Selected the one file the change touches; it also covers the owner-scoping, bounded-excerpt and regex-deadline behaviour of the same search path.",
  "exclusions": "No full suites. Test-only change: no src/ file is touched, so no other Python suite can observe it. tests/v3/test_harnesses.py is excluded even though it holds the sibling 'instruction' not in search assertion: that assertion already passes unchanged on main, and two of the file's cases (test_chat_rolls_over_to_current_command_runtime_without_mutating_frozen_session[False|True]) fail identically on main with AttributeError: 'Commands' object has no attribute 'manager' at harnesses.py:5618. tests/v3/test_harness_adapters.py::test_claude_native_capabilities_exclude_project_files_and_shell also fails on main and is tracked separately; it is unrelated to this payload contract. No frontend or Playwright entries: no UI file, style or rendered surface changed, and no UI code reads untrusted_data.",
  "expected_tests": {
    "python": 32,
    "frontend": 0,
    "native": 0,
    "playwright": 0
  },
  "python": [
    "tests/v3/test_automation_runtime.py"
  ],
  "frontend": [],
  "native": [],
  "playwright": []
}
