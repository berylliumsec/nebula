name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  python:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install Poetry
        run: python -m pip install "poetry==2.1.3"
      - name: Validate package metadata
        run: poetry check
      - name: Install project
        run: poetry install --no-interaction
      - name: Test
        run: poetry run pytest -q tests/v3
      - name: Lint Nebula 3
        run: poetry run ruff check src/nebula/v3 tests/v3
      - name: Check Nebula 3 formatting
        run: poetry run ruff format --check src/nebula/v3 tests/v3
      - name: Audit diagnostic blind spots
        if: matrix.python-version == '3.12'
        run: poetry run python scripts/audit_diagnostic_blind_spots.py --python --rust
      - name: Type-check Nebula 3
        if: matrix.python-version == '3.12'
        run: poetry run mypy src/nebula/v3
      - name: Build distributions
        run: poetry build

  database-migrations:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: nebula_migrations
          POSTGRES_PASSWORD: nebula
          POSTGRES_USER: nebula
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U nebula -d nebula_migrations"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      NEBULA_TEST_POSTGRES_URL: postgresql+psycopg://nebula:nebula@127.0.0.1:5432/nebula_migrations
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install "poetry==2.1.3"
      - run: poetry install --with dev --no-interaction
      - name: Exercise SQLite and PostgreSQL upgrade/downgrade paths
        run: >-
          poetry run pytest -q tests/v3/test_migrations.py

  rootless-podman:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"
          cache: pip
      - name: Install rootless Podman
        run: |
          sudo apt-get update
          sudo apt-get install -y podman uidmap slirp4netns fuse-overlayfs
          podman info --format '{{.Host.Security.Rootless}}' | grep -Fx true
      - run: python -m pip install "poetry==2.1.3"
      - run: poetry install --with dev --no-interaction
      - name: Build minimal operator-runtime integration image
        run: >-
          podman build
          --file tests/v3/integration/Containerfile.operator-runtime
          --tag localhost/nebula-operator-runtime:ci
          .
      - name: Exercise real disposable execution and workspace persistence
        env:
          NEBULA_TEST_CONTAINER_RUNTIME: /usr/bin/podman
          NEBULA_TEST_RUNTIME_IMAGE: localhost/nebula-operator-runtime:ci
        run: >-
          poetry run pytest -q tests/v3/test_sandbox_integration.py

  frontend:
    runs-on: ubuntu-latest
    env:
      POETRY_VIRTUALENVS_IN_PROJECT: "true"
    defaults:
      run:
        working-directory: ui
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"
          cache: pip
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: ui/package-lock.json
      - run: python -m pip install "poetry==2.1.3"
        working-directory: .
      - run: poetry install --only main --no-interaction
        working-directory: .
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run lint --if-present
      - run: npm run audit:diagnostics
      - run: npm test
      - run: npm run build
      - name: Prepare lightweight real-Core runtime fixture
        working-directory: .
        run: |
          docker pull --platform=linux/amd64 docker.io/kalilinux/kali-rolling:latest
          repository_digest="$(docker image inspect docker.io/kalilinux/kali-rolling:latest --format '{{index .RepoDigests 0}}')"
          image_digest="${repository_digest##*@}"
          source_reference="docker.io/kalilinux/kali-rolling@${image_digest}"
          derived_tag="localhost/nebula-kali-headless:v4-${image_digest#sha256:}"
          echo "NEBULA_KALI_SOURCE_IMAGE=${source_reference}" >> "${GITHUB_ENV}"
          docker build \
            --build-arg "KALI_SOURCE=${source_reference}" \
            --file tests/v3/integration/Containerfile.real-core-runtime \
            --tag "${derived_tag}" \
            .
      - run: npm run test:e2e

  desktop-macos:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: ui/package-lock.json
      - uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30
        with:
          toolchain: stable
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"
          cache: pip
      - run: npm --prefix ui ci
      - run: npm --prefix ui run build
      - run: python -m pip install "poetry==2.1.3"
      - run: poetry install --with dev --no-interaction
      - name: Test macOS Docker Desktop and Podman Machine command boundaries
        run: poetry run pytest -q tests/v3/test_sandbox.py -k macos
      - name: Prepare compile-time sidecar boundary
        run: |
          target="$(rustc -vV | sed -n 's/^host: //p')"
          mkdir -p ui/src-tauri/binaries
          mkdir -p build/nebula-core-metadata
          printf 'Generated CI fixture for native shell tests.\n' > build/nebula-core-metadata/THIRD_PARTY_NOTICES.txt
          touch "ui/src-tauri/binaries/nebula-core-$target"
          chmod +x "ui/src-tauri/binaries/nebula-core-$target"
      - name: Test native menus and sidecar boundary
        run: cargo test --locked --manifest-path ui/src-tauri/Cargo.toml

  desktop:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: ui/package-lock.json
      - uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30
        with:
          toolchain: stable
      - name: Set deterministic QA build identity
        run: |
          printf 'NEBULA_BUILD_COMMIT=%s\n' "$(git rev-parse HEAD)" >> "$GITHUB_ENV"
          printf 'NEBULA_BUILD_TARGET=%s\n' "$(rustc -vV | sed -n 's/^host: //p')" >> "$GITHUB_ENV"
          printf 'NEBULA_BUILD_TIMESTAMP=%s\n' "$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_ENV"
          printf 'NEBULA_DISTRIBUTION_CHANNEL=managed\n' >> "$GITHUB_ENV"
          printf 'NEBULA_DISTRIBUTION=managed\n' >> "$GITHUB_ENV"
      - name: Install Tauri system libraries
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf xvfb
      - run: python -m pip install "poetry==2.1.3"
      - run: poetry install --with dev --no-interaction
      - run: python scripts/nebula3_version.py check
      - run: poetry run pytest -q tests/v3
      - run: poetry run pytest -q packaging/updater/test_generate_manifest.py
      - run: npm --prefix ui ci
      - run: npm --prefix ui run build
      - run: poetry run python -m scripts.build_nebula_core
      - name: Smoke test frozen Core sidecar
        run: |
          sidecar="$(find ui/src-tauri/binaries -maxdepth 1 -type f -name 'nebula-core-*' -print -quit)"
          test -n "$sidecar"
          test -x "$sidecar"
          "$sidecar" doctor --data-dir "$RUNNER_TEMP/nebula-sidecar-smoke"
          poetry run python scripts/package_audit.py "$sidecar"
      - name: Test managed desktop shell
        run: |
          cargo clippy --locked --manifest-path ui/src-tauri/Cargo.toml --all-targets -- -D warnings
          cargo test --locked --manifest-path ui/src-tauri/Cargo.toml
      - name: Compile direct updater boundary
        env:
          NEBULA_UPDATE_CHANNEL: prerelease
          NEBULA_UPDATE_ENDPOINT: https://example.invalid/updates/prerelease/latest.json
          NEBULA_UPDATER_PUBLIC_KEY: RWQAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
        run: cargo test --locked --features direct-updater --manifest-path ui/src-tauri/Cargo.toml
      - name: Build managed DEB installer
        env:
          NEBULA_DISTRIBUTION: managed
        run: npm --prefix ui run tauri -- build --ci --bundles deb --config src-tauri/tauri.managed.conf.json
      - name: Normalize Debian prerelease ordering
        run: |
          deb="$(find ui/src-tauri/target/release/bundle/deb -maxdepth 1 -type f -name '*.deb' -print -quit)"
          poetry run python -m scripts.repack_deb "$deb" --version "$(cat NEBULA3_VERSION)"
      - name: Inspect and self-test installed layout
        run: |
          deb="$(find ui/src-tauri/target/release/bundle/deb -maxdepth 1 -type f -name '*.deb' -print -quit)"
          test -n "$deb"
          dpkg-deb --info "$deb"
          expected="$(poetry run python -c 'from pathlib import Path; from scripts.repack_deb import debian_version; print(debian_version(Path("NEBULA3_VERSION").read_text().strip()))')"
          test "$(dpkg-deb --field "$deb" Version)" = "$expected"
          dpkg-deb --extract "$deb" "$RUNNER_TEMP/nebula-deb"
          test -x "$RUNNER_TEMP/nebula-deb/usr/bin/nebula"
          test -x "$RUNNER_TEMP/nebula-deb/usr/bin/nebula-core"
          test -x "$RUNNER_TEMP/nebula-deb/usr/bin/nebula-ui"
          poetry run python scripts/package_audit.py --tree "$RUNNER_TEMP/nebula-deb"
          xvfb-run -a "$RUNNER_TEMP/nebula-deb/usr/bin/nebula-ui" --self-test
          xvfb-run -a poetry run python scripts/test_desktop_crash_cleanup.py "$RUNNER_TEMP/nebula-deb/usr/bin/nebula-ui"

  container:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - run: docker build --pull=false -f Dockerfile.v3 -t nebula-v3:ci .
      - name: Smoke test non-root Core
        run: |
          docker run -d --name nebula-v3-ci -e NEBULA_V3_API_TOKEN=test-token -p 127.0.0.1:18000:8000 nebula-v3:ci
          trap 'docker rm -f nebula-v3-ci' EXIT
          for attempt in {1..30}; do
            curl --fail --silent --header 'Authorization: Bearer test-token' http://127.0.0.1:18000/api/v1/health && exit 0
            sleep 1
          done
          docker logs nebula-v3-ci
          exit 1

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: anchore/sbom-action@fbfd9c6c189226748411491745178e0c2017392d # v0.20.10
        with:
          path: .
          format: spdx-json
          output-file: nebula.spdx.json
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sbom
          path: nebula.spdx.json
