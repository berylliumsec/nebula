# Workspace tool recovery — 2026-09-09

Journey: workspace search through the browser/harness gateway returns bounded useful results; operator opens activity, sees real tool identity and outcome, expands details, reloads without duplicate rows. Core owns tool receipts and durable harness events. The linked host directory owns file permissions; URL selects the session; the UI reducer merges call identity. No research commands from the reported turn may be replayed.

Invariants: literal traversal has an independent total time/byte/file budget; regex evaluation has its own smaller per-evaluation ceiling. Exhaustion returns partial results and narrowing guidance, never a complete/no-match claim. Explicit files and directories are accepted; parent traversal and symlinks remain rejected. Generated directory exclusions are disclosed and can be bypassed only by selecting their explicit path. Permission failures preserve accessible matches, nonzero command exit codes, and captured stdout/stderr.

Activity start/update/completion must share toolCallId, recover nested MCP server/tool and underlying errors, and preserve succeeded/failed/cancelled outcomes. Older saved payloads are interpreted at read/render time without rewriting research records. Out-of-order starts may fill identity but must not regress terminal status. Details remain expandable. No generic completion label may hide a failed workspace search.

Applicable lifecycle: discover/search, result paging, failure/narrow/retry, activity refresh/reload and out-of-order updates. Streaming research execution, tool replay, project deletion and permission escalation are excluded. Tests: focused workspace service budget/path tests, synthetic Grok normalization tests, activity reducer/components, production real-Core saved-activity browser journey on desktop/mobile Chromium and WebKit plus LAN. No physical device is available.

Live diagnosis: turn 3f7e6d99-83ed-4426-94c6-d4239fbe9790 completed, with 80 terminal harness tool events: eight failed and 72 completed. Five workspace searches hit the shared 250ms deadline; two file paths were rejected by directory-only validation. One command retained exit 2, 4,181 stdout bytes and 20,150 stderr bytes (180 permission-denied lines). Docker is rootless; container UID 65532 maps to host UID 165531, whereas sampled linked workspace files are UID/GID 1000 and mode 0600. The /workspace bind mount points at the correct original project folder. This is identity mapping incompatibility, not evidence of Nebula having changed those files' ownership. Do not chmod/chown the project, copy the workspace, or elevate the runtime to work around it.

Queue steering: place the existing durable follow-up queue inside the composer. Default to one compact count/status disclosure; expand for edit/reorder/pause/clear. Errors and needs-review entries force visible recovery. Queue state remains Core-owned; collapse state is transient. Preserve all queue mutation/refresh semantics and keyboard/touch labels. Test collapsed geometry with long text, expansion and actionable errors, then production desktop/mobile rendering.

Verification: 7/7 focused backend tests passed, including a 1,200-file sparse/no-match workspace, literal versus regex, file/directory paths, time/byte/file exhaustion, pagination, permissions, symlink/traversal rejection and preserved command receipt artifacts. The Grok test additionally exercises synthetic start/completion through the actual ACP adapter. UI checks: 27/27 passed (activity reducer/ledger and queue). TypeScript/Vite production build, Ruff and git diff check passed. Final committed real-Core browser journey passed 8/8 over http://192.168.1.155:19433: desktop Chromium 1440/1024, emulated Android Chromium 320/390/430 and emulated iPhone WebKit 320/390/430. Checked paused queue below 52px inside composer, expand/collapse, saved failure labels, one activity row after reload, no horizontal overflow and composer Axe scan. Evidence: /tmp/nebula-workspace-final; build log /tmp/nebula-workspace-build-final.log. Physical devices were unavailable.

Remaining boundary: the existing rootless command worker still cannot read owner-private host files because UID 65532 maps to host UID 165531. No file ownership, mount location, runtime privilege or research contents were changed. Built-in workspace search/read use Core's host identity; command failures now retain exit status and partial artifacts with actionable access guidance. A transparent read/write identity solution for rootless Docker requires a separately designed non-elevating mapping; this work does not claim that every recursive shell search can read private host files. No reported research command was replayed.
