# Remove workspace quotas and their recursive scans

Operator journey: link a large existing host folder, open Files or Terminal, upload a file, and use reviewed execution. Folder size and entry count must not disable those workflows or trigger periodic quota scans. Core owns project/workspace identity; the original filesystem owns storage capacity and permissions; the browser owns transient selection. No copies, permission changes, or research commands are needed.

Remove workspace-wide total-size, entry-count and individual-file quotas, including execution admission, execution monitoring, terminal readiness and upload accounting. Preserve atomic streamed writes, overwrite/concurrency checks, project containment, symlink protection and linked-folder reset protection. Request-level search, listing, preview and process-resource bounds remain independent. Existing filesystem change reporting is not a quota and remains outside this change.

Applicable lifecycle: discovery/readiness, terminal start, streamed upload/save/reload, failed write cleanup, overwrite retry and execution cancellation. No changes to approval semantics, provider execution, browser research or runtime identity. Verify no recursive walk occurs on terminal capability checks or uploads; prove large existing files and >50,000 entries no longer block entry. Tests: focused service/API cases, UI readiness and workspace components, committed production real-Core Files upload/refresh workflow on desktop/mobile Chromium and WebKit over LAN. Actual bind-mount acceptance should inspect a large folder without running research commands. Physical device testing is unavailable.

Verification: 8/8 selected backend checks and 22/22 terminal/workspace component checks passed. Backend cases reject recursive walking during terminal readiness and upload; large-folder terminal preflight includes 50,001 entries and a 6 GiB sparse file. Idle supervision, exact reviewed execution, atomic overwrite, concurrent modification and symlink/traversal protections passed. Ruff, diff check and TypeScript/Vite production build passed. The committed real-Core upload/refresh journey passed 8/8 desktop Chromium (1440/1024), emulated Android Chromium (320/390/430) and emulated iPhone WebKit (320/390/430) profiles over http://192.168.1.155:19436, with the large existing folder, Axe and overflow checks. Evidence: /tmp/nebula-quota-browser; build /tmp/nebula-quota-build.log. Physical devices unavailable.

Actual Docker bind-mount check: /home/agent/apple_bug_bounty_program mounted read-only at /workspace in 0.271 seconds; exit 0. Used the already-installed image, network none, read-only root, dropped capabilities, no-new-privileges and UID/GID 65532. Only tested that /workspace is a directory; no traversal, host mutations, or research commands. Receipt: /tmp/nebula-quota-mount.json. This proves mount admission, not access to every owner-private file or performance of arbitrary recursive commands.
