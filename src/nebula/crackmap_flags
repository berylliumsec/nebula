-h: show this help message and exit
--help: show this help message and exit
-t: set how many concurrent threads to use (default 100)
THREADS: set how many concurrent threads to use (default 100)
--timeout: max timeout in seconds of each thread (default None)
TIMEOUT: max timeout in seconds of each thread (default None)
--jitter: sets a random delay between each connection (default None)
INTERVAL: sets a random delay between each connection (default None)
--no-progress: Not displaying progress bar during scan
--verbose: enable verbose output
--debug: enable debug level information
--version: Display CME version
ftp: own stuff using FTP
ldap: own stuff using LDAP
mssql: own stuff using MSSQL
rdp: own stuff using RDP
smb: own stuff using SMB
ssh: own stuff using SSH
vnc: own stuff using VNC
winrm: own stuff using WINRM
