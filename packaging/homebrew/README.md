# Homebrew status

The current release workflow builds Linux x86_64 DEB and AppImage artifacts only.
It does not build macOS DMGs or publish a Homebrew cask. The former cask template
is no longer in this directory, so there is no Homebrew promotion step to run.
See the [release matrix](../RELEASING.md) for supported artifacts.
