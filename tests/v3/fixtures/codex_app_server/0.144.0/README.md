These schemas were generated with `codex-cli 0.144.0` using:

```text
codex app-server generate-json-schema --out <directory>
```

Only the stable request/notification schemas exercised by the Nebula adapter are
checked in. Experimental schema generation was intentionally not enabled.
